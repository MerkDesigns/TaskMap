// The coarse backdrop is capped at 2 DPR. These dimensions retain ample pan space for the
// deterministic canvas-element dataset and remain useful for its bounds tests.
export const BENCHMARK_WORLD_WIDTH = 3600;
export const BENCHMARK_WORLD_HEIGHT = 3000;
export const BENCHMARK_LIQUID_MAX_DPR = 2;
